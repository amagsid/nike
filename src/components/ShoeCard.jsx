const ShoeCard = ({ imgURL, ChangeBigShoeImage, bigShoeImg }) => {
	return (
		<div className={`border-2 rounded-xl flex flex-1 flex-contianer`}>
			{' '}
			<h1> shoe</h1>{' '}
		</div>
	);
};

export default ShoeCard;
